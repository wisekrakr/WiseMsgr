<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wisekrakr.wisemessenger">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WiseMessenger">
        <activity android:name=".app.activity.profile.ProfileActivity"></activity>
        <activity android:name=".app.activity.actions.SearchActivity" />
        <activity android:name=".app.activity.auth.LoginMobileActivity" />
        <activity android:name=".app.activity.actions.CreateGroupActivity" />
        <activity android:name=".app.activity.chat.GroupChatActivity" />
        <activity android:name=".app.activity.profile.ProfileSettingsActivity" />
        <activity android:name=".app.activity.chat.PrivateChatActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.wisekrakr.wisemessenger.app.activity.HomeActivity" />
        </activity>
        <activity android:name=".app.activity.auth.LoginActivity" />
        <activity android:name=".app.activity.auth.RegisterActivity" />
        <activity android:name=".app.activity.StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- HOME -->
        <activity android:name=".app.activity.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>